# Colegio API (estructura tipo sistema-main)

- Paquetes con nombres en español: **Entidades, Repositorios, Servcios, Controladores, Security, Dto**.
- Modelo igual al diagrama (alumno, docente, materia, grado, seccion, periodo_lectivo, curso, matricula, calificacion).
- Endpoints base: `/api/alumnos`, `/api/cursos`, `/api/matriculas`, `/api/calificaciones`.
- Swagger: `/swagger`.

> Ajusta `application.properties` a tu motor/credenciales.
